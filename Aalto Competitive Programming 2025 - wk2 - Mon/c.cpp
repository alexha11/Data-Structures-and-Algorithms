#include <bits/stdc++.h>
using namespace std;

const int MAXN = 1001;
vector <string> a;
vector <int> f[MAXN];

void solve() {
 int n, m;
 cin >> n >> m;
 for (int i = 0; i < n; i++) cin >> a[i];
 for (int i = 0; i < n; i++) {
     for (int j = 0; j < m; j++) {
 }
 
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    solve();

    return 0;
}
